@* Centralized interactive render mode.
   NOTE: Razor only auto-imports `_Imports.razor`. This file is informational.
   The actual global interactive mode lives in `_Imports.razor`.
   Remove per-page @rendermode directives to avoid duplication and potential conflicts. *@
@rendermode InteractiveServer
